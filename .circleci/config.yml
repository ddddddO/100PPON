version: 2
jobs:
  build:
    docker:
      - image: python:3

    steps:
      - checkout
      - run:
          name: Run unittest
          command: |
            pwd
            ls
            echo $CIRCLE_WORKING_DIRECTORY
            export PYTHONPATH=$CIRCLE_WORKING_DIRECTORY/v2/chapter3; cd v2; make testci