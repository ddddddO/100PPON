version: 2
jobs:
  build:
    docker:
      - image: python:3

    steps:
      - checkout
      - run:
          name: Run unittest
          command: |
            cd v2; export PYTHONPATH=$CIRCLE_WORKING_DIRECTORY/chapter3; make testci